import React from 'react'
import { Title, Grid, Row } from '../store'

const Skill = () => {

  return (
    <section className="skill">
      <Grid>
        <Row>
          <Title mainTitle="Mes compétences" color="white" />
        </Row>
      </Grid>
    </section>
  )
}

export default Skill
